---
phase: 16
plan: 05
type: execute
wave: 3
depends_on:
  - 16-03
  - 16-04
files_modified:
  - frontend/src/components/bots/types.ts
  - frontend/src/components/bots/AddBotModal.tsx
  - frontend/src/components/bots/PlatformSelector.tsx
  - frontend/src/components/bots/BotCard.tsx
  - frontend/src/components/bots/QRCodeModal.tsx
  - frontend/src/components/bots/BotDetailPanel.tsx
  - frontend/src/services/api.ts
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Discord platform shows as supported (not grayed out) in the platform selector"
    - "AddBotModal accepts a Discord bot_token input and creates a Discord bot"
    - "Discord bots can be started, stopped, and deleted from the UI"
    - "BotCard and QRCodeModal show platform-aware messaging for Discord (no QR code)"
    - "Discord bots appear in the bot list with correct status"
  artifacts:
    - path: "frontend/src/components/bots/types.ts"
      provides: "Discord platform marked as supported"
      contains: "supported: true"
    - path: "frontend/src/components/bots/AddBotModal.tsx"
      provides: "Discord bot_token input flow"
      contains: "discord"
    - path: "frontend/src/services/api.ts"
      provides: "Discord API calls if needed"
  key_links:
    - from: "frontend/src/components/bots/AddBotModal.tsx"
      to: "frontend/src/services/api.ts"
      via: "botApi.createWorkspace call with platform='discord'"
      pattern: "botApi\\.createWorkspace"
    - from: "frontend/src/components/bots/BotCard.tsx"
      to: "frontend/src/services/api.ts"
      via: "botApi.startWorkspace/stopWorkspace calls"
      pattern: "botApi\\.(start|stop)Workspace"
---

<objective>
Wire Discord into the frontend bot management UI -- platform selector, AddBotModal, BotCard, status display, start/stop/delete buttons.

Purpose: Users need to create and manage Discord bots from the same UI as WhatsApp/Telegram bots. Discord uses bot_token auth (like Telegram), no QR code needed.
Output: Updated frontend components with Discord support enabled and platform-aware UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16/16-RESEARCH.md
@.planning/phases/16/16-CONTEXT.md

<interfaces>
From frontend/src/components/bots/types.ts:
```typescript
export type Platform = 'whatsapp' | 'telegram' | 'discord' | 'messenger' | 'instagram' | 'teams' | 'twitter';
export const PLATFORM_CONFIG: Record<Platform, { label: string; color: string; icon: string; supported: boolean }> = {
  discord:   { label: 'Discord',   color: '#5865F2', icon: 'Headphones',    supported: false },
  // ...
};
```

From frontend/src/services/api.ts (botApi):
```typescript
export const botApi = {
  createWorkspace(name: string, config: any): Promise<Workspace>
  startWorkspace(id: string): Promise<any>
  stopWorkspace(id: string): Promise<any>
  getConnectionStatus(id: string): Promise<any>
  deleteWorkspace(id: string): Promise<any>
  // ...
}
```

From frontend/src/components/bots/AddBotModal.tsx:
- Step flow: platform -> config -> connect
- Telegram has two auth types: 'token' and 'phone'
- canNext check: `(platform !== 'telegram' || tgAuthType === 'phone' || botToken.trim().length > 0)`
- Connect step: shows QR code for WhatsApp, shows loading/connected for Telegram
- handleNext: calls `botApi.createWorkspace(name, { platform, ...config, ...(platform === 'telegram' ? { bot_token: botToken } : {}) })`

From frontend/src/components/bots/BotCard.tsx:
- Shows platform icon, name, status, start/stop/delete buttons
- Platform-aware rendering based on PLATFORM_CONFIG
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable Discord in platform config and update AddBotModal</name>
  <files>frontend/src/components/bots/types.ts, frontend/src/components/bots/AddBotModal.tsx, frontend/src/components/bots/PlatformSelector.tsx</files>
  <read_first>
    - frontend/src/components/bots/types.ts (PLATFORM_CONFIG with discord.supported = false)
    - frontend/src/components/bots/AddBotModal.tsx (full file -- platform selection, config step, connect step)
    - frontend/src/components/bots/PlatformSelector.tsx (platform grid rendering)
    - .planning/phases/16/16-CONTEXT.md (Frontend Changes section)
  </read_first>
  <action>
**In frontend/src/components/bots/types.ts:**
Change `discord.supported` from `false` to `true` in PLATFORM_CONFIG.

**In frontend/src/components/bots/AddBotModal.tsx:**
Add Discord support following the Telegram token flow pattern:

1. In the config step (`step === 'config'`), add a Discord-specific section after the Telegram section:
   - Show a bot_token input field (like Telegram token mode)
   - Label: "Bot Token"
   - Placeholder: "MTI4NTk0MzI4..." (Discord bot tokens are long base64-like strings)
   - Help text: "Get a token from the Discord Developer Portal > Bot > Token. Enable Message Content Intent under Privileged Gateway Intents."
   - No QR code needed -- Discord uses direct token auth

2. Update the `canNext` check to allow Discord with bot_token:
   - Change from: `(platform !== 'telegram' || tgAuthType === 'phone' || botToken.trim().length > 0)`
   - Change to: `((platform !== 'telegram' && platform !== 'discord') || (platform === 'telegram' && tgAuthType === 'phone') || botToken.trim().length > 0)`

3. In `handleNext` (config -> connect step), pass bot_token for Discord:
   - Change the createWorkspace call to include bot_token for Discord: `...(platform === 'telegram' || platform === 'discord' ? { bot_token: botToken } : {})`

4. In the connect step (`step === 'connect'`), Discord behaves like Telegram token flow:
   - After createWorkspace, Discord bots are already connected (like Telegram)
   - Set `connectStatus = 'connected'` immediately after successful creation
   - No QR code polling needed
   - Update the condition from `if (platform === 'telegram')` to `if (platform === 'telegram' || platform === 'discord')` for the "already connected" path

5. In the connect step display, add Discord-specific messaging:
   - When creating: "Connecting to Discord..."
   - When connected: "Discord bot is connected!"
   - No QR code display for Discord (same as Telegram)

6. In `resetAndClose`, no changes needed -- botToken is already reset.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `types.ts` PLATFORM_CONFIG discord entry has `supported: true`
    - `AddBotModal.tsx` shows bot_token input for Discord platform in config step
    - `AddBotModal.tsx` canNext allows proceeding with bot_token for Discord
    - `AddBotModal.tsx` passes bot_token in createWorkspace call for Discord
    - `AddBotModal.tsx` treats Discord like Telegram in connect step (no QR, instant connected)
    - TypeScript compiles cleanly
  </acceptance_criteria>
  <done>Discord platform is selectable, AddBotModal accepts bot_token input, and connect flow works without QR code.</done>
</task>

<task type="auto">
  <name>Task 2: Update BotCard, QRCodeModal, and BotDetailPanel for Discord</name>
  <files>frontend/src/components/bots/BotCard.tsx, frontend/src/components/bots/QRCodeModal.tsx, frontend/src/components/bots/BotDetailPanel.tsx</files>
  <read_first>
    - frontend/src/components/bots/BotCard.tsx (full file -- platform-aware rendering, status display, buttons)
    - frontend/src/components/bots/QRCodeModal.tsx (full file -- QR display, platform messaging)
    - frontend/src/components/bots/BotDetailPanel.tsx (full file -- bot detail view, platform-aware)
    - .planning/phases/16/16-CONTEXT.md (Frontend Changes section)
  </read_first>
  <action>
**In frontend/src/components/bots/BotCard.tsx:**
Ensure Discord bots display correctly:
1. Platform icon uses 'Headphones' from PLATFORM_CONFIG (already defined in types.ts)
2. Status badge colors work for Discord (connected=green, disconnected=gray, error=red)
3. Start/Stop/Delete buttons work for Discord bots (no special handling needed -- they use the same botApi endpoints)
4. If the card has any WhatsApp/Telegram-specific text (like "Scan QR"), make it platform-aware: show "Bot token connected" for Discord

**In frontend/src/components/bots/QRCodeModal.tsx:**
Update messaging for Discord platform:
1. If the modal shows platform-specific instructions, add Discord messaging:
   - "Discord bot is connecting..." (loading state)
   - "Discord bot is connected!" (success state)
   - Do NOT show QR code for Discord -- the modal should show a loading spinner then success
2. Check if the modal receives platform prop; if not, check the bot's platform from the API response

**In frontend/src/components/bots/BotDetailPanel.tsx:**
Ensure the detail panel works for Discord bots:
1. Platform name displays correctly (already uses PLATFORM_CONFIG)
2. Connection status section works (no QR code for Discord)
3. Start/Stop buttons function correctly
4. Configuration section shows relevant fields (no QR-related fields for Discord)
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `BotCard.tsx` renders Discord bots with correct platform icon and status
    - `BotCard.tsx` start/stop/delete buttons work for Discord bots
    - `QRCodeModal.tsx` does NOT show QR code for Discord platform
    - `QRCodeModal.tsx` shows platform-aware messaging for Discord (loading/connected states)
    - `BotDetailPanel.tsx` displays Discord bot details correctly
    - All frontend TypeScript compiles cleanly
  </acceptance_criteria>
  <done>BotCard, QRCodeModal, and BotDetailPanel all handle Discord platform correctly -- no QR code, platform-aware messaging, working CRUD buttons.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> API | Authenticated via Clerk JWT (or bypass in dev) |
| Frontend form -> bot_token field | User inputs Discord bot token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-11 | Information Disclosure | bot_token in frontend state | accept | Token is transient in React state, sent to backend via HTTPS, never stored in localStorage |
| T-16-12 | Information Disclosure | bot_token visible in input field | mitigate | Use type="password" for the bot_token input field |
| T-16-SC | Tampering | npm packages | mitigate | No new packages installed |
</threat_model>

<verification>
- Frontend TypeScript compilation passes
- Discord platform is selectable and not grayed out
- AddBotModal creates Discord bots with bot_token
- No QR code shown for Discord bots
</verification>

<success_criteria>
- `frontend/src/components/bots/types.ts` has `discord.supported: true`
- AddBotModal accepts Discord bot_token and creates bot
- BotCard shows Discord bots with correct status and working buttons
- QRCodeModal shows correct messaging for Discord (no QR)
- `cd frontend && npx tsc --noEmit` passes
</success_criteria>

<output>
Create `.planning/phases/16/16-05-SUMMARY.md` when done
</output>
