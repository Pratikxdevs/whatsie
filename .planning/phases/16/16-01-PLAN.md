---
phase: 16
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/adapters/discordAdapter.ts
  - package.json
  - package-lock.json
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "DiscordAdapter.connect() creates a discord.js Client and logs in"
    - "DiscordAdapter.disconnect() destroys the Client and removes from Map"
    - "DiscordAdapter.sendMessage() fetches channel and sends text via WebSocket"
    - "DiscordAdapter.isReady() returns true when connected and false otherwise"
    - "DiscordAdapter.healthCheck() returns connection status for a bot"
  artifacts:
    - path: "src/adapters/discordAdapter.ts"
      provides: "Discord client lifecycle management + send"
      exports: ["DiscordAdapter"]
    - path: "package.json"
      provides: "discord.js dependency"
      contains: "discord.js"
  key_links:
    - from: "src/adapters/discordAdapter.ts"
      to: "discord.js"
      via: "Client import and instantiation"
      pattern: "new Client.*GatewayIntentBits"
    - from: "src/adapters/discordAdapter.ts"
      to: "src/debug/server.ts"
      via: "addLog for lifecycle events"
      pattern: "addLog.*DISCORD"
---

<objective>
Install discord.js and create the DiscordAdapter class that manages multiple Discord bot Client instances in-process.

Purpose: This is the foundational integration layer -- all subsequent Discord features (normalizer, worker, routes, frontend) depend on a working adapter.
Output: `src/adapters/discordAdapter.ts` with static methods for connect/disconnect/send/healthCheck/isReady, discord.js added to package.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16/16-RESEARCH.md
@.planning/phases/16/16-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/normalizer/types.ts:
```typescript
export interface NormalizedMessage {
  tenantId: string;
  platform: 'whatsapp' | 'telegram' | 'discord' | 'meta' | 'teams' | 'twitch' | 'twitter';
  userId: string;
  message: { text: string | null; attachments: Array<{...}>; quickReplies: Array<{...}> | null };
  type: 'text' | 'image' | 'video' | 'audio' | 'file' | 'location' | 'contact' | 'sticker' | 'button_click';
  timestamp: string;
  metadata: { raw: any; replyTo: string | null; isForwarded: boolean; mentions: string[]; [key: string]: any };
}
```

From src/adapters/telegramAdapter.ts (pattern to follow):
- Static methods: sendMessage(sessionName, chatId, text)
- Uses addLog() from ../debug/server
- Uses logger from ../config/logger
- Error interceptor pattern

From src/debug/server.ts:
```typescript
export function addLog(level: string, msg: string, code?: string, meta?: Record<string, unknown>)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install discord.js and create DiscordAdapter</name>
  <files>src/adapters/discordAdapter.ts, package.json, package-lock.json</files>
  <read_first>
    - src/adapters/telegramAdapter.ts (adapter pattern: static methods, logger, addLog)
    - src/adapters/telegram.adapter.ts (thin wrapper pattern)
    - src/adapters/whatsapp.adapter.ts (existing adapter pattern)
    - src/normalizer/types.ts (NormalizedMessage type)
    - src/debug/server.ts (addLog signature)
    - .planning/phases/16/16-RESEARCH.md (Pattern 1: DiscordAdapter example, anti-patterns, lifecycle)
  </read_first>
  <action>
Run `npm install discord.js` to add the dependency. Then create `src/adapters/discordAdapter.ts` implementing the DiscordAdapter class with:

1. A module-level `Map<string, Client>` named `clients` keyed by botId.
2. Import `{ Client, GatewayIntentBits, ChannelType, TextChannel, DMChannel }` from discord.js, plus logger from ../config/logger and addLog from ../debug/server.
3. Static methods:
   - `connect(botToken: string, botId: string): Promise<void>` -- calls disconnect first to clean up existing client, creates new Client with intents [Guilds, GuildMessages, DirectMessages, MessageContent], attaches clientReady/shardDisconnect/shardReconnecting/shardError event listeners (all using addLog with DISCORD_LIFECYCLE/DISCORD_AUTH codes), stores in Map, calls `client.login(botToken)`.
   - `disconnect(botId: string): Promise<void>` -- calls `client.destroy()`, deletes from Map, logs via addLog.
   - `getClient(botId: string): Client | undefined` -- returns from Map.
   - `isReady(botId: string): boolean` -- checks `client?.isReady() ?? false`.
   - `healthCheck(botId: string): { connected: boolean; tag?: string }` -- returns connection status and bot user tag.
   - `sendMessage(channelId: string, text: string, botId: string): Promise<void>` -- fetches channel via `client.channels.fetch(channelId)`, checks `channel.isTextBased()`, calls `channel.send(text)`, logs via addLog with DISCORD_SEND code.

Per D-01 (discord.js v14), D-02 (4 GatewayIntents), D-03 (Map<string, Client> pattern), D-04 (channel.send pattern), D-06 (adapter architecture with static methods).
  </action>
  <verify>
    <automated>npx tsc --noEmit src/adapters/discordAdapter.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies contains `"discord.js": "^14.26.4"` or similar v14.x version
    - `src/adapters/discordAdapter.ts` exists and exports class `DiscordAdapter`
    - File contains `import { Client, GatewayIntentBits` from discord.js
    - File contains `GatewayIntentBits.Guilds, GatewayIntentBits.GuildMessages, GatewayIntentBits.DirectMessages, GatewayIntentBits.MessageContent`
    - File contains `static async connect(botToken: string, botId: string): Promise<void>`
    - File contains `static async disconnect(botId: string): Promise<void>`
    - File contains `static async sendMessage(channelId: string, text: string, botId: string): Promise<void>`
    - File contains `static isReady(botId: string): boolean`
    - File contains `static healthCheck(botId: string)`
    - File contains `const clients = new Map<string, Client>()`
    - File contains `addLog` calls with codes starting with `DISCORD_`
    - File contains `client.destroy()` in disconnect method
    - File contains `channel.isTextBased()` check before send
    - `npx tsc --noEmit` exits 0 (no type errors)
  </acceptance_criteria>
  <done>DiscordAdapter class is implemented with connect/disconnect/send/isReady/healthCheck static methods, discord.js installed, TypeScript compiles cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord Gateway -> Node.js process | Inbound messages arrive via authenticated WebSocket (bot token proves identity) |
| User input -> bot_token field | User-supplied bot token is used to authenticate with Discord API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Information Disclosure | bot_token storage | mitigate | Mask token in logs (addLog calls), store masked version in config JSON per existing pattern |
| T-16-02 | Information Disclosure | bot_token in error messages | mitigate | Catch and sanitize errors before logging, never include full token in error output |
| T-16-SC | Tampering | npm install discord.js | mitigate | Package legitimacy audit passed in RESEARCH.md [OK] |
</threat_model>

<verification>
- TypeScript compilation passes: `npx tsc --noEmit`
- discord.js is in package.json dependencies
- DiscordAdapter exports all required static methods
- No hardcoded tokens or secrets in the adapter file
</verification>

<success_criteria>
- `src/adapters/discordAdapter.ts` exists with all 6 static methods
- `npm ls discord.js` shows installed version
- `npx tsc --noEmit` passes with zero errors
</success_criteria>

<output>
Create `.planning/phases/16/16-01-SUMMARY.md` when done
</output>
