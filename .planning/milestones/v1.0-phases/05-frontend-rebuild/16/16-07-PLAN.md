---
phase: 16
plan: 07
type: execute
wave: 5
depends_on:
  - 16-05
  - 16-06
files_modified:
  - src/__tests__/discord-adapter.test.ts
  - src/__tests__/discord-normalizer.test.ts
  - src/__tests__/discord-worker.test.ts
  - src/__tests__/discord-workspace.test.ts
  - src/__tests__/discord-rate-limit.test.ts
  - src/__tests__/discord-startup.test.ts
  - src/__tests__/response-router.test.ts
  - src/__tests__/setup.ts
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "DiscordAdapter.connect creates Client and calls login"
    - "DiscordAdapter.disconnect destroys Client and removes from Map"
    - "DiscordAdapter.sendMessage sends to correct channel"
    - "normalizeDiscordMessage maps Discord Message to NormalizedMessage"
    - "normalizeDiscordMessage rejects bot messages (author.bot === true)"
    - "ResponseRouter.dispatch handles platform='discord'"
    - "Rate limiter enforces Discord platform limits"
    - "discordWorker processes jobs through the 13-step pipeline (DISC-06)"
    - "Startup sync reconnects Discord bots on server boot (DISC-10)"
    - "All existing tests still pass"
  artifacts:
    - path: "src/__tests__/discord-adapter.test.ts"
      provides: "DiscordAdapter unit tests"
    - path: "src/__tests__/discord-normalizer.test.ts"
      provides: "Discord normalizer unit tests"
    - path: "src/__tests__/discord-workspace.test.ts"
      provides: "Discord workspace route integration tests"
    - path: "src/__tests__/discord-rate-limit.test.ts"
      provides: "Discord rate limiter tests"
    - path: "src/__tests__/discord-worker.test.ts"
      provides: "Discord worker pipeline tests (DISC-06)"
    - path: "src/__tests__/discord-startup.test.ts"
      provides: "Discord startup sync tests (DISC-10)"
    - path: "src/__tests__/response-router.test.ts"
      provides: "Updated ResponseRouter tests with Discord case"
    - path: "src/__tests__/setup.ts"
      provides: "discord.js mock added to test setup"
  key_links:
    - from: "src/__tests__/discord-adapter.test.ts"
      to: "src/adapters/discordAdapter.ts"
      via: "vi.mock discord.js, test static methods"
      pattern: "vi\\.mock.*discord\\.js"
    - from: "src/__tests__/response-router.test.ts"
      to: "src/router/index.ts"
      via: "Test Discord case in dispatch"
      pattern: "case.*discord"
---

<objective>
Write comprehensive tests for all Discord integration components and verify the full E2E flow works.

Purpose: Validate that the Discord adapter, normalizer, rate limiter, ResponseRouter, and workspace routes all work correctly. Catch regressions in existing tests.
Output: Test files for Discord adapter, normalizer, workspace routes, rate limiting. Updated test setup with discord.js mock. Updated response-router tests with Discord case.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16/16-RESEARCH.md
@.planning/phases/16/16-CONTEXT.md

<interfaces>
From src/__tests__/setup.ts (existing test infrastructure):
```typescript
// Prisma mock, Redis mock, BullMQ mock
// process.env vars for test
export { mockPrisma, redisStore };
```

From src/__tests__/response-router.test.ts (existing test pattern):
- Tests dispatch for whatsapp and telegram platforms
- Mocks prisma, adapters, SessionManager

From .planning/phases/16/16-RESEARCH.md (Test Framework):
- Framework: Vitest 4.x
- Quick run: `npx vitest run src/__tests__/discord-adapter.test.ts`
- Full suite: `npm test`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add discord.js mock to test setup and create adapter/normalizer tests</name>
  <files>src/__tests__/setup.ts, src/__tests__/discord-adapter.test.ts, src/__tests__/discord-normalizer.test.ts</files>
  <read_first>
    - src/__tests__/setup.ts (full file -- existing Prisma/Redis/BullMQ mocks)
    - src/__tests__/telegram-adapter.test.ts (test pattern to follow)
    - src/adapters/discordAdapter.ts (full file -- methods to test)
    - src/normalizer/discord.ts (full file -- function to test)
    - src/normalizer/types.ts (NormalizedMessage interface)
    - .planning/phases/16/16-RESEARCH.md (Validation Architecture, test map)
  </read_first>
  <action>
**In src/__tests__/setup.ts:**
Add a discord.js mock after the existing BullMQ mock:
```typescript
vi.mock('discord.js', () => {
  const mockClient = {
    login: vi.fn().mockResolvedValue('token'),
    destroy: vi.fn(),
    isReady: vi.fn().mockReturnValue(true),
    on: vi.fn(),
    once: vi.fn(),
    user: { tag: 'TestBot#1234', id: '123456789' },
    channels: {
      fetch: vi.fn().mockResolvedValue({
        isTextBased: vi.fn().mockReturnValue(true),
        send: vi.fn().mockResolvedValue({ id: 'msg-1' }),
      }),
    },
  };
  return {
    Client: vi.fn().mockImplementation(() => mockClient),
    GatewayIntentBits: { Guilds: 1, GuildMessages: 2, DirectMessages: 4, MessageContent: 8 },
    ChannelType: { GuildText: 0, DM: 1 },
  };
});
```

**Create src/__tests__/discord-adapter.test.ts:**
Test DiscordAdapter static methods. Import the adapter AFTER the mock is applied. Tests:
1. `connect` -- creates Client with correct intents, calls login, stores in Map
2. `connect` -- disconnects existing client before creating new one (calls destroy)
3. `disconnect` -- calls destroy, removes from Map
4. `disconnect` -- no-op if client doesn't exist (no error)
5. `getClient` -- returns client from Map
6. `getClient` -- returns undefined if not found
7. `isReady` -- returns true when client is ready
8. `isReady` -- returns false when client doesn't exist
9. `sendMessage` -- fetches channel and calls channel.send(text)
10. `sendMessage` -- throws if client not connected
11. `sendMessage` -- throws if channel is not text-based (isTextBased returns false)

Each test: `vi.clearAllMocks()` in beforeEach, use the mocked Client from discord.js.

**Create src/__tests__/discord-normalizer.test.ts:**
Test `normalizeDiscordMessage` function. Create mock Discord Message objects. Tests:
1. Maps text message to NormalizedMessage with `platform: 'discord'`
2. Sets `userId` to `message.author.id`
3. Sets `metadata.channelId`, `metadata.guildId`, `metadata.messageId`
4. Throws on bot messages (`author.bot === true`)
5. Classifies image attachments (contentType `image/png` -> type 'image')
6. Classifies video/audio/file attachments correctly
7. Sets `replyTo` from `message.reference.messageId`
8. Extracts mentions from `message.mentions.users`
9. Handles message with empty content (`text = null`)
10. Handles message with no attachments (empty collection)

Mock message structure:
```typescript
const mockMessage = {
  id: 'msg-123',
  content: 'Hello world',
  author: { id: 'user-456', username: 'testuser', bot: false },
  channelId: 'channel-789',
  guildId: 'guild-101',
  createdTimestamp: Date.now(),
  attachments: new Map(),
  mentions: { users: new Map() },
  reference: null,
};
```
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/discord-adapter.test.ts src/__tests__/discord-normalizer.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `src/__tests__/setup.ts` contains `vi.mock('discord.js', ...)` with Client, GatewayIntentBits
    - `src/__tests__/discord-adapter.test.ts` exists with 11 tests covering all static methods
    - `src/__tests__/discord-normalizer.test.ts` exists with 10 tests covering normalizer
    - Tests cover bot message rejection, attachment classification, metadata mapping
    - `npx vitest run src/__tests__/discord-adapter.test.ts` exits 0
    - `npx vitest run src/__tests__/discord-normalizer.test.ts` exits 0
  </acceptance_criteria>
  <done>Discord adapter and normalizer have comprehensive unit tests. Test setup includes discord.js mock.</done>
</task>

<task type="auto">
  <name>Task 2: Create workspace, rate limit, and response router tests</name>
  <files>src/__tests__/discord-workspace.test.ts, src/__tests__/discord-rate-limit.test.ts, src/__tests__/response-router.test.ts</files>
  <read_first>
    - src/__tests__/response-router.test.ts (full file -- existing tests to extend)
    - src/routes/workspaces.ts (Discord branches added in plan 03)
    - src/rateLimiter/index.ts (discordLimiter added in plan 03)
    - src/__tests__/setup.ts (mock patterns)
    - .planning/phases/16/16-RESEARCH.md (Test map: DISC-07, DISC-08, DISC-09)
  </read_first>
  <action>
**Create src/__tests__/discord-workspace.test.ts:**
Integration tests for Discord workspace routes using supertest or direct route handler testing:
1. POST /api/workspaces with platform='discord' requires bot_token (400 without)
2. POST /api/workspaces with platform='discord' creates bot and returns 201
3. POST /api/workspaces/:id/start calls DiscordAdapter.connect
4. POST /api/workspaces/:id/stop calls DiscordAdapter.disconnect
5. GET /api/workspaces/:id/connection-status returns Discord bot status
6. DELETE /api/workspaces/:id calls DiscordAdapter.disconnect

Mock DiscordAdapter.connect/disconnect/isReady and prisma bot queries.

**Create src/__tests__/discord-rate-limit.test.ts:**
Test Discord rate limiters:
1. discordLimiter exists and is a RedisRateLimiter instance
2. discordLimiter uses 200ms spacing
3. sendWithRateLimit('discord', fn) executes the function
4. sendWithRateLimit('discord', fn) throws for unknown platform

**Create src/__tests__/discord-worker.test.ts:**
Integration tests for the discordWorker BullMQ worker:
1. Worker accepts jobs from 'discord-messages' queue
2. Worker processes jobs through 13-step pipeline (SessionManager load → DB updates → billing → event log → Socket.IO → session push → workflow check → intent → workflow trigger → rules → AI fallback → outbound)
3. Worker emits 'failed' event on error
4. Worker uses concurrency 200 (per locked decision D-7)

Mock BullMQ Worker, SessionManager, prisma, IntentClassifier, RuleEngine, AIBridge, ResponseRouter.

**Create src/__tests__/discord-startup.test.ts:**
Tests for Discord bot reconnection on server startup:
1. Finds all Discord bots with status='connected' in DB
2. Calls DiscordAdapter.connect() for each found bot
3. Logs reconnection attempts
4. Handles connect failures gracefully (logs error, updates bot status to 'error')

Mock prisma.bot.findMany, DiscordAdapter.connect, logger.

**Update src/__tests__/response-router.test.ts:**
Add Discord case to existing ResponseRouter tests:
1. dispatch with platform='discord' sends via DiscordAdapter.sendMessage
2. dispatch with platform='discord' throws if no connected bot found
3. dispatch with platform='discord' extracts channelId from metadata

Add `vi.mock('../adapters/discordAdapter')` to the existing mocks.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/discord-workspace.test.ts src/__tests__/discord-rate-limit.test.ts src/__tests__/response-router.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `src/__tests__/discord-workspace.test.ts` exists with tests for all workspace route handlers
    - `src/__tests__/discord-rate-limit.test.ts` exists with tests for discord limiter
    - `src/__tests__/discord-worker.test.ts` exists with tests for discordWorker 13-step pipeline
    - `src/__tests__/discord-startup.test.ts` exists with tests for Discord startup sync reconnect
    - `src/__tests__/response-router.test.ts` updated with Discord case tests
    - All Discord test files pass: `npx vitest run src/__tests__/discord-*.test.ts` exits 0
    - All existing tests still pass: `npm test` exits 0
  </acceptance_criteria>
  <done>Comprehensive test coverage for Discord integration: adapter, normalizer, workspace routes, rate limiting, and ResponseRouter dispatch. Full test suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner -> Production code | Tests run against mocked dependencies only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-SC | Tampering | npm packages | mitigate | No new packages installed |
</threat_model>

<verification>
- All Discord test files pass
- All existing tests still pass
- Full test suite green
</verification>

<success_criteria>
- `npx vitest run src/__tests__/discord-*.test.ts` exits 0
- `npm test` exits 0 (no regressions)
- Test coverage for: adapter connect/disconnect/send, normalizer bot rejection + mapping, workspace routes CRUD, rate limiting, ResponseRouter dispatch
</success_criteria>

<output>
Create `.planning/phases/16/16-07-SUMMARY.md` when done
</output>
